# 설정
.env 파일에 
```
FILE_PATH = "data/dataset.csv"
SAVE_PATH = "data/vecs"
```
가 존재해야 합니다.

# 출력 예시

app.py로 실행합니다.

```
불편을 드려 죄송합니다. 아래 내용을 참고해 보세요.

[원인]

Groq의 function calling 형식이 OpenAI와 미세하게 달라서 툴 호출 형식 불일치로 'Invalid tool call' 에러가 발생합니다. 특히 bind_tools를 사용할 때 이 형식 차이가 더 문제를 일으키는 경향이 있습니다.

[해결책]

- 시스템 메시지로 툴 사용(함수 호출) 형식을 명시하세요. prompt의 system role에 Groq가 기대하는 툴 호출 방식(함수 이름, 파라미터 구조 등)을 분명히 적어 모델이 해당 형식으로 호출하도록 유도합니다.
- bind_tools 사용을 점검하거나 일시적으로 비활성화해 보세요. bind_tools가 호출 형식을 변형하는 경우가 있으므로 Groq 형식에 맞게 호출을 직접 구성하거나 bind_tools를 조정합니다.
- 툴 호출 로직을 Groq의 function calling 규격에 맞게 변환하도록 코드 또는 래퍼를 수정하고, 간단한 샘플 호출로 동작을 확인합니다.
- 계속 문제 발생 시 Groq 호출 포맷(정확한 함수/파라미터 예시)을 system message에 반영한 뒤 재검증하세요.
긴 문서 때문에 불편하셨겠네요. 아래와 같이 원인과 해결책을 정리해 드립니다.

[원인]

- Groq API를 사용하는 모델의 컨텍스트 윈도우(토큰 제한)가 초과되었습니다. (예: 8k 토큰 제한)

[해결책]

- MapReduce 방식으로 처리: 문서를 적절히 분할한 뒤 각 청크를 요약(map)하고, 요약들을 합쳐 최종 답변을 생성(reduce)합니다.
- 더 긴 컨텍스트를 지원하는 모델 사용: Llama-3-70b-8192처럼 긴 컨텍스트 버전 모델을 사용하여 더 많은 토큰을 처리합니다.

위 두 방법을 조합하면 더 긴 문서도 효율적으로 처리할 수 있습니다.
```
